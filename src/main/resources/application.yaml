server:
  port: 8080
  ssl:
    enabled: false
    key-store: file:/home/diego_borray/keystore.p12
    key-store-password: Xcargo_2024
    key-store-type: PKCS12
    key-alias: xcargo_ssl  

spring:
  datasource:
    url: jdbc:mysql:///x-cargo?cloudSqlInstance=x-cargo-prod:us-east1:bd-rrhh-xcargo&socketFactory=com.google.cloud.sql.mysql.SocketFactory&useSSL=false&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: xcargo_app
    password: Xcargo_2024

  jpa:
    show-sql: true
    generate-ddl: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
        naming:
          implicit-strategy: org.hibernate.boot.model.naming.ImplicitNamingStrategyLegacyJpaImpl
          physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

logging:
  level:
    root: INFO
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG


management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
